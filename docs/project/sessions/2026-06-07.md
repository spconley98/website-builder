# Session Log — 2026-06-07

**Agent:** Claude Sonnet 4.6 · **Contributor:** Sean · **Phase:** setup + architecture (pre-scaffold)

Extensive summary of everything done this session, the problems hit, and where we're headed.

---

## What we built (in order)

1. **GitHub repo** — created `spconley98/website-builder` (public, shared). Added **Matt** (`mp214gitty`)
   as a write collaborator (invite pending acceptance).
2. **`context-transfer` skill** — copied from AAS-WEBSITE and **adapted** for this project: optional
   health-check, no hardcoded notebook, and **per-contributor attribution** (shared project — tag who
   did what; default Sean).
3. **NotebookLM brain** — created `website-builder-brain`
   (`bd83690f-e997-46c5-b054-6ff3139e11d6`), shared with Matt (`mpitto214@gmail.com`, pending).
4. **Obsidian vault** — Sean pointed a vault at the project folder (visual map / second brain).
   Matt must install Obsidian to use it.
5. **Reference library** (`docs/_reference-library/`) — copied 6 AI reference docs from VR-REDESIGN
   (AI Build Bible, Custom SDKs Bible, 4 Comprehensive_* guides), each disclaimed "reference, NOT
   scaffold." Built a **3-tier convention** per doc: `(Raw Text)` / `(Mind Map)` / `(Visualization)`,
   generated via NotebookLM (mind-map notes → nested bullets; professional infographics).
6. **Master Skills Catalog** — researched + wrote a full catalog: Sean's global skills, project-local
   skills (deduped — only Supabase ones were unique), plugins, plus Anthropic / VoltAgent / Karpathy /
   Jack Roberts recommended skills with real download links. Also given the 3-tier treatment.
7. **`reference-visualizer` skill** — proactive-offer skill: when a new reference-library doc appears,
   it **asks first**, then generates the mind-map + visualization. Branches: Yes / No / **Unsure → log
   to `_PENDING_APPROVALS.md` for Sean-only approval.**
8. **Firecrawl MCP** — wired into local `.mcp.json`; set up `.gitignore` + `.mcp.json.example` so the
   key never hits the repo. Confirmed key valid but **account out of credits**; documented the
   restart-to-load + REST-fallback behavior.
9. **Multi-agent constitution** — single-source-of-truth: **`AGENTS.md`** (canonical) +
   **`CLAUDE.md`** / **`GEMINI.md`** (thin pointers) + root **`MEMORY.md`** (portable state). Front
   door for Claude / Codex / Gemini and for Matt.
10. **Architecture via `/grill-me`** — pressure-tested the lead-pipeline design across 10 decisions →
    documented in `docs/project/ARCHITECTURE.md`. Approved; scaffold to be built next session.

---

## The project vision (short)
A **local-AI lead pipeline**: find local businesses with **no website**, prioritize by how much photo
material exists online (Yelp/Google), output clickable lead lists → Sean + Matt build + sell them
websites. Local AI (Ollama/RTX 3090) does the grunt research. Full detail: `ARCHITECTURE.md`.

---

## Problems / friction hit
- **Firecrawl out of credits** — key authenticates, scrapes blocked. MCP only loads on restart; REST is
  the mid-session fallback.
- **Windows shell quirks** — bash heredoc/quoting flaky; switched folder ops to PowerShell and JSON
  conversion to temp Python scripts.
- **NotebookLM CLI gotchas** — `generate mind-map` has no `--wait`; `note get` wraps JSON in a header
  that must be stripped; `source add` uses positional content + `-n`. All encoded in `reference-visualizer`.
- **Model not chosen** — Sean will pick the Ollama model later; scaffold stays model-agnostic.
- **Google Places** — net-new paid dependency (cheap) for the acquisition layer.

---

## Where we're headed (next session)
1. **Build the approved scaffold** (tree in `ARCHITECTURE.md` §5) — files, stubs, CLI, schema, config.
2. Sean **picks the Ollama model**.
3. Sean + Matt get **Google Places + Firecrawl keys** (own keys each).
4. Implement **Lead Finder**, then **Lead Prioritizer**.

> 🚫 Nothing past project setup gets built until **Sean approves** (`AGENTS.md` §0).
