# Matt Context Transfers

Matt's agent writes end-of-session handoffs here.

Use one file per session:

```text
YYYY-MM-DD-HHMM-short-topic.md
```

Shared `MEMORY.md` should not be rewritten by Matt's agent unless Sean explicitly approves that exact
shared-memory update. If Matt's agent thinks shared memory is stale, it should write the proposed
change in the session handoff here.
